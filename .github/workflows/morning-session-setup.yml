name: Morning Session Setup

on:
  schedule:
    - cron: '0 6 * * *'  # 06:00 UTC = 07:00 CET
  workflow_dispatch:

jobs:
  setup-session:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Get current date
        id: date
        run: echo "CURRENT_DATE=$(date -u +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: Create session branch (if not exists)
        run: |
          BRANCH_NAME="session-${{ env.CURRENT_DATE }}"
          echo "➡️  Overujem/vytváram branch: $BRANCH_NAME"
          git checkout $BRANCH_NAME || git checkout -b $BRANCH_NAME
          git pull origin $BRANCH_NAME || true # Skúsi stiahnuť zmeny, ak branch už existuje

      - name: Copy session to development
        continue-on-error: false
        run: |
          echo "📂 Kopírujem staging/sessions/today/session.md do development/sessions/current/"
          mkdir -p development/sessions/current
          
          if [ ! -f "staging/sessions/today/session.md" ]; then
            echo "⚠️  staging/sessions/today/session.md neexistuje, vytváram základný súbor"
            mkdir -p staging/sessions/today
            SESSION_DATE="${{ env.CURRENT_DATE }}"
            cat > staging/sessions/today/session.md << EOF
# Session: ${SESSION_DATE}

## 🎯 Cieľ Dňa
*Doplň svoj cieľ pre dnešný deň*

## 📝 Poznámky
*Tvoje poznámky z práce*

## ✅ Dokončené Úlohy
*Zoznam dokončených úloh*

## 🔄 V Práci
*Úlohy, na ktorých práve pracuješ*
EOF
          fi
          
          cp staging/sessions/today/session.md development/sessions/current/session.md
          echo "✅ Session skopírovaná"

      - name: Add log entry for new session setup
        continue-on-error: true
        run: |
          python -c "
          import sys
          sys.path.insert(0, '.')
          from scripts.utils.log_manager import add_log_entry
          
          add_log_entry(
              action_name='Session Setup: Nová denná session pre ${{ env.CURRENT_DATE }}',
              status='Completed',
              files_changed=['development/sessions/current/session.md'],
              xp_estimate=5.0
          )
          print('✅ Log entry added')
          " || echo "⚠️  Nepodarilo sa pridať log entry (pokračujeme)"

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "chore(session): daily session setup for ${{ env.CURRENT_DATE }}" || echo "⚠️  Commit failed (možno žiadne zmeny)"
          git push origin session-${{ env.CURRENT_DATE }} || echo "⚠️  Push failed (možno žiadne zmeny)"

      - name: Cleanup (optional, if you want to remove the temporary session branch)
        run: |
          # Tato cast sa spusti az po uspesnom mergi pull requestu
          # Zatial ju nebudem implementovat, kedze merge sa deje uz v auto-session-rotation
          echo "ℹ️  Cleanup branch sa bude riesit po mergi Pull Requestu z predosleho workflow."
